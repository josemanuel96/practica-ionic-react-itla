import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.josemanuel.practicaionic',
  appName: 'Practica Ionic Multiplataforma',
  webDir: 'dist'
};

export default config;
